export default async function handler(req,res){const k=process.env.TOMTOM_API_KEY;if(!k)return res.status(503).json({error:'TOMTOM_API_KEY missing'});const lat=Number(req.query.lat),lng=Number(req.query.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return res.status(400).json({error:'coordinates required'});const u='https://api.tomtom.com/traffic/services/4/flowSegmentData/absolute/12/json?key='+encodeURIComponent(k)+'&point='+lat+','+lng+'&unit=kmph';try{const r=await fetch(u);res.status(r.status).json(await r.json())}catch(e){res.status(502).json({error:'TomTom unavailable'})}}